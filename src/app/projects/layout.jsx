import React from 'react'

export const metadata = {
    title: "AlifCodes - Projects",
    description: "Your Vision, My Code",
  };

export default function layout({children}) {
  return (
    <div>
        {children}
    </div>
  )
}
