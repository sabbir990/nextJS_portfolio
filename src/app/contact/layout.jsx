import React from 'react'

export const metadata = {
    title: "AlifCodes - Contact",
    description: "Your Vision, My Code",
};

export default function layout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}
